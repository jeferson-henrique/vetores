#include <stdio.h>
int main()
{
    int i;
    int vetor[11];
    
    for(i=1; i<=10; i++){
    printf("insira uma nota\n");
    scanf("%d",&vetor[i]);
    }
    printf("\n\n");
    i = 0;
    for(i = 0; i < 10; i++)
        printf("%d ,", vetor[i]);
}
